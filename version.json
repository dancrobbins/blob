{
  "version": "1.0.0",
  "updates": [
    "Selection bounds are now computed from the DOM (rendered blob positions) and converted to world coords, fixing incorrect or oversized selection rectangles."
  ]
}
